<form
  x-data
  hx-get={`https://api.telegram.org/bot${import.meta.env.TELEGRAM}/sendMessage?chat_id=${import.meta.env.CHAT}`}
  hx-swap="none"
>
  <label for="text">
    Ваш номер
    <input
      id="phone"
      name="text"
      type="tel"
      x-mask="+7 (999) 999-99-99"
      placeholder="+7 (___) ___-__-__"
    />
  </label>
  <button hx-swap="none" type="submit">Записаться</button>
</form>
<style>
  form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
    width: 100%;

    button {
      width: 90%;
      height: 3rem;
      color: white;
      font-weight: bold;
      font-size: 1rem;
      text-shadow: 0 0 4px rgb(0 0 0 / 50%);
      background: #dfb114ff;
      border: 0;
      border-radius: 2rem;
      cursor: pointer;

      &:hover {
        background: #dfb114cc;
      }
    }

    input {
      height: 2rem;
      width: 11rem;
      padding: 0.5rem 1rem;
      font-size: 1rem;
      background: #121212;
      border: 0;
      border-radius: 0.2rem;

      &:focus {
        outline: 0.1rem solid #ababab;
      }
    }
  }
</style>
